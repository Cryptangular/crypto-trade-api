import { SettingsValidationPipe } from './settings-validation.pipe';

describe('SettingsValidationPipe', () => {
  it('should be defined', () => {
    expect(new SettingsValidationPipe()).toBeDefined();
  });
});
