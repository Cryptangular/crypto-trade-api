CORS_ORIGIN=https://stonks.com.ru
HOST=0.0.0.0
PORT=3000

# Database Configuration
DATABASE_URL=

# JWT Authentication Configuration
JWT_ACCESS_SECRET=
JWT_ACCESS_EXPIRATION=

JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRATION=

# Settings
# 32-byte (256-bit) encryption key for AES-256. Command for generation(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=
